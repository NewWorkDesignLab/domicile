public enum PlayerRole
{
    spectator = 0,
    guide = 1,
    learner = 2
};

public enum SessionTarget
{
    unspecified = 0,
    create = 1,
    join = 2,
    offline = 3
};

public enum Gender
{
    unspecified = 0,
    female = 1,
    male = 2,
    divers = 3
};

public enum RoomCount
{
    unspecified = 0,
    two = 2,
    three = 3
};

public enum TextureDifficulty
{
    unspecified = 0,
    easy = 1,
    medium = 2,
    hard = 3
};

public enum CaseReport
{
    unspecified = 0,
    auto = 1,
    manual = 2
};

public enum Tenant
{
    unspecified = 0,
    one = 1,
    two = 2,
    three = 3
};

public enum RentalContract
{
    unspecified = 0,
    one = 1,
    two = 2,
    three = 3
};

public enum HandoverProtocol
{
    unspecified = 0,
    one = 1,
    two = 2,
    three = 3
};